// =============================================================================
// API Configuration
// =============================================================================

/**
 * Flag de alternancia mock/real.
 * Cambiar a `false` cuando el backend esté desplegado y listo.
 */
export const USE_MOCK_API = true;

/** URL base del attendance-service */
export const ATTENDANCE_API_URL = 'https://api.example.com/attendance';

/** URL base del auth-service */
export const AUTH_API_URL = 'https://api.example.com/auth';

/** Retardo simulado para el mock client (ms) */
export const MOCK_DELAY_MS = 300;
